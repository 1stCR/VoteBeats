# =============================================================================
# VoteBeats Server - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values.
# Do NOT commit .env to version control.
# =============================================================================

# --- Server Configuration ---
# Port the Express server listens on
# Default: 3002
PORT=3002

# Node environment: 'development', 'production', or 'test'
NODE_ENV=development

# --- Database Configuration ---
# Path to SQLite database file (absolute or relative to server directory)
# Production default: ./data/votebeats.db (mount persistent volume at /app/data)
# Development default: ./votebeats.db
# DB_PATH=./votebeats.db

# --- Authentication ---
# Secret key used to sign and verify JWT tokens
# IMPORTANT: Use a strong, random string in production (min 32 characters)
# Example: openssl rand -hex 32
JWT_SECRET=change-me-to-a-strong-random-secret

# --- CORS Configuration ---
# Comma-separated list of allowed origins for cross-origin requests
# Default allows localhost development servers
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# --- Spotify OAuth Integration (optional) ---
# Required only if you want to enable Spotify playlist export features
# Get credentials from: https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=your-spotify-client-id
SPOTIFY_CLIENT_SECRET=your-spotify-client-secret
SPOTIFY_REDIRECT_URI=http://localhost:3002/api/spotify/callback

# --- Custom Domain Configuration (optional) ---
# When deploying with a custom domain (e.g., votebeats.com), update:
# 1. Add your custom domain to CORS_ORIGIN above:
#    CORS_ORIGIN=http://localhost:3000,https://votebeats.com,https://www.votebeats.com
# 2. Update SPOTIFY_REDIRECT_URI to use your custom domain:
#    SPOTIFY_REDIRECT_URI=https://votebeats.com/api/spotify/callback
# 3. The server also dynamically reads custom domain from the database
#    (configured via DJ Settings > Domain Setup in the web UI)
# Note: CORS is also dynamically updated when you configure a domain in the UI
