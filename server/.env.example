# =============================================================================
# VoteBeats Server - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values.
# Do NOT commit .env to version control.
# =============================================================================

# --- Server Configuration ---
# Port the Express server listens on
# Default: 3002
PORT=3002

# Node environment: 'development', 'production', or 'test'
NODE_ENV=development

# --- Authentication ---
# Secret key used to sign and verify JWT tokens
# IMPORTANT: Use a strong, random string in production (min 32 characters)
# Example: openssl rand -hex 32
JWT_SECRET=change-me-to-a-strong-random-secret

# --- CORS Configuration ---
# Comma-separated list of allowed origins for cross-origin requests
# Default allows localhost development servers
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# --- Spotify OAuth Integration (optional) ---
# Required only if you want to enable Spotify playlist export features
# Get credentials from: https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=your-spotify-client-id
SPOTIFY_CLIENT_SECRET=your-spotify-client-secret
SPOTIFY_REDIRECT_URI=http://localhost:3002/api/spotify/callback
